{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if command -v py >/dev/null 2>&1; then py -3 \"${CLAUDE_PLUGIN_ROOT}/hooks/doc_gate.py\"; else python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/doc_gate.py\"; fi",
            "shell": "bash",
            "timeout": 20
          }
        ]
      }
    ]
  }
}
