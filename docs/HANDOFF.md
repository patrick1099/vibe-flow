# 交接 · 2026-09-24

**在做**：试行"架构层只看"。用户懂架构概念（模块、数据流、流程图），不懂具体语言和代码。0.13.2 已把「我打算怎么搭」图改回好档多模块时默认画、架构词首次带解释即可用（`skills/vibe-flow/SKILL.md` §4、`clarify-needs/references/run-model.md`、README「分工」与「沿革」）。

**试行的规矩**：架构层照旧由 AI（加 codex）定，但每次都画给用户看、附一两句理由，不等回复就开工；用户插话就照改。代码层（语言、库、HTML 怎么写）照旧不问用户。

**回头看的节点**：试行两三个**真正的大活**（好档、涉及多个模块）之后，问用户一次：这段时间插话多不多？
- 常插话 → 把"架构层的取舍给两三条路、由用户拍板"写进 §4 决策边界；
- 基本不插话 → 保持"只看"，删掉 README「沿革」里"这是试行"那句和本文件。

**别碰**："方案定稿后不等回复就开工"不变。
