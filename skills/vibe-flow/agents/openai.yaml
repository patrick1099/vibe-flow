interface:
  display_name: "Vibe Flow"
  short_description: "判一次档再干活：小事要省，大事要好"
  default_prompt: "Use $vibe-flow first, before writing any code: size this one change (small and clear → act directly; wide-impact, costly-if-wrong, or genuinely unclear → thorough), then route to the right skill and scale clarification, implementation and verification to that size."
